package com.net.nky.service.push;

//import org.junit.Assert;
//import org.junit.Test;

public class PushUtilTest {

//	@Test
	public void testgetPushClient() {
//		Assert.assertTrue(PushUtil.createTag("testTag")); //可重复执行
//		Assert.assertTrue(PushUtil.deleteTag("testTag"));
//		Assert.assertTrue(PushUtil.createTag("testTag")); //可重复执行
//		Assert.assertTrue(PushUtil.createTag("testTag")); //可重复执行
//		Assert.assertNotNull(PushUtil.queryTags("testTag", null));
//		Assert.assertNotNull(PushUtil.queryTags("testTag", null).size() == 1);
//		Assert.assertTrue(0 >= PushUtil.queryDeviceNumInTag("testTag"));//没有就是-1
//		Assert.assertTrue(0 == PushUtil.addDevicesToTag("testTag", new String[] { "3830725647211086088" }).size());
//		Assert.assertTrue(0 == PushUtil.addDevicesToTag("testTag", new String[] { "3830725647211086088" }).size());
//		Assert.assertTrue(0 == PushUtil.addDevicesToTag("testTag", new String[] { "3774826707009314924" }).size());
//		Assert.assertTrue(1 == PushUtil.queryDeviceNumInTag("testTag"));//没有就是-1
//		Assert.assertTrue(1 == PushUtil.queryDeviceNumInTag("testTag"));//没有就是-1
//		String msgId = PushUtil.pushMsgToTag("testTag", "pushMsgToTag");
		//通过在Demo中点击设置标签，可以给当前设备打上一个标签。
		//您可以通过推送控制台或者SDK对您设置的标签进行标签组播的推送，所有设置为相同标签名称的设备都会收到该标签名的标签组播推送。
//		Assert.assertNotNull(msgId);//没有就是-1
//		Assert.assertNotNull(PushUtil.queryMsgStatus(msgId));//没有就是-1   3830725647211086088
//		Assert.assertNotNull(PushUtil.pushMsgToSingleDevice("3830725647211086088", "标题", "内容你1好"));//没有就是-1
//		Assert.assertNotNull(PushUtil.pushMsgToSingleDevice("3774826707009314924", "标题", "内容你好"));//没有就是-1
//
//		Assert.assertTrue(0 == PushUtil.deleteDevicesFromTag("testTag", new String[] { "3830725647211086088" }).size());
		//addDevicesToTag
		//
	}

}
